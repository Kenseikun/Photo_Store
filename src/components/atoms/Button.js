import React from "react";
import styled, { css } from "styled-components";

const StyledButton = styled.div`
  /* background-color: ${({ color }) => color}; */
`;

const Button = () => {
  return <StyledButton>PROPS PRODUCT CATEGORY/</StyledButton>;
};

export default Button;
